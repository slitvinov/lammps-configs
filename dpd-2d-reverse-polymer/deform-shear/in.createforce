fix		idnve all nve
variable        R equal 1
fix		deffix all deform 1 xy erate ${R} remap v

variable bodyfx atom mass*${gx}*((y>${ysize}/2.0)-(y<${ysize}/2.0))
fix reverce_periodic all addforce v_bodyfx 0.0 0.0 
